<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jaxrs="http://cxf.apache.org/jaxrs"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans
         https://www.springframework.org/schema/beans/spring-beans.xsd
         http://cxf.apache.org/jaxrs
         https://cxf.apache.org/schemas/jaxrs.xsd"
>

    <import resource="classpath:META-INF/cxf/cxf.xml"/>

    <bean id="objectMapper" class="com.fasterxml.jackson.databind.ObjectMapper"/>

    <bean id="jacksonProvider" class="com.fasterxml.jackson.jakarta.rs.json.JacksonJsonProvider">
        <property name="mapper" ref="objectMapper"/>
    </bean>

    <bean id="corsInterceptor" class="com.navadeep.ChatApplication.utils.CorsInterceptor">
        <property name="allowOrigin" value="*"/>
        <property name="allowCredentials" value="false"/>
        <property name="allowedMethods" value="GET,POST,PUT,DELETE,OPTIONS"/>
        <property name="allowedHeaders" value="Origin,Content-Type,Accept,Authorization,Content-Disposition,X-Requested-With"/>
        <property name="maxAge" value="3600"/>
    </bean>

    <!-- Define the JAX-RS server endpoint -->
    <jaxrs:server id="authServer" address="/auth">
        <jaxrs:serviceBeans>
            <ref bean="authController" />
        </jaxrs:serviceBeans>
        <jaxrs:providers>
            <ref bean="jacksonProvider"/>
            <bean class="org.apache.cxf.jaxrs.provider.MultipartProvider"/>
        </jaxrs:providers>

        <jaxrs:inInterceptors>
            <ref bean="corsInterceptor"/>
        </jaxrs:inInterceptors>
        <jaxrs:outInterceptors>
            <ref bean="corsInterceptor"/>
        </jaxrs:outInterceptors>

    </jaxrs:server>


</beans>
